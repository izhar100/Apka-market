const initState={
    isLoading:false,
    isAuth:false,
    token:"",
    isError:false
}
export const reducer=(state=initState,{type,payload})=>{
    switch(type){
        default:{
            return state;
        }
    }
}