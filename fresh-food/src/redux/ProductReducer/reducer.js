const initState={
    isLoading:false,
    product:[],
    isError:false
}
export const reducer=(state=initState,{type,payload})=>{
    switch(type){
        default:{
            return state;
        }
    }
}